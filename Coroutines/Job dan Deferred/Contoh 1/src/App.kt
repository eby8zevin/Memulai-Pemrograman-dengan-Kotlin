import kotlinx.coroutines.*

fun main() = runBlocking {

    // menggunakan launch()
    val jobLaunch = launch {
        // do background task her }
    }

    // menggunakan job()
    val job = Job()
}